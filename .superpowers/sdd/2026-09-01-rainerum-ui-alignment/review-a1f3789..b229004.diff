diff --git a/src/app/globals.css b/src/app/globals.css
index 1374117..1daa0a8 100644
--- a/src/app/globals.css
+++ b/src/app/globals.css
@@ -11,7 +11,7 @@
   --surface-soft: #fff0ee;
   --paper: var(--surface);
   --line: #dbbeba;
-  --line-strong: #b8908c;
+  --line-strong: #8f706c;
   --focus: #004075;
   --accent: var(--brand);
   --success: #1f7a4d;
@@ -30,36 +30,36 @@
 
 /* Unified atomic Admin timetable workspace. */
 .admin-bottom-nav { display: none; }
-.admin-timetable-workspace { display: grid; max-width: none; gap: 0; padding: 0; background: #f8f9fb; }
-.admin-timetable-heading { display: flex; align-items: end; justify-content: space-between; gap: 1rem; padding: 1rem; border-bottom: 1px solid #daddE4; background: white; }
+.admin-timetable-workspace { display: grid; max-width: none; gap: 0; padding: 0; background: var(--page); }
+.admin-timetable-heading { display: flex; align-items: end; justify-content: space-between; gap: 1rem; padding: 1rem; border-bottom: 1px solid var(--line); background: white; }
 .admin-timetable-heading h1 { margin: 0; font-size: clamp(1.2rem, 4vw, 1.7rem); }
-.admin-timetable-heading .eyebrow { color: #0753a6; }
-.admin-draft-status { display: grid; justify-items: end; color: #566172; font-size: 0.72rem; }
+.admin-timetable-heading .eyebrow { color: var(--brand); }
+.admin-draft-status { display: grid; justify-items: end; color: var(--muted); font-size: 0.72rem; }
 .admin-draft-status strong { color: #8a6000; }
 .admin-timetable-heading__actions { display: flex; align-items: end; gap: 0.8rem; }
 .admin-timetable-heading__actions > .button { min-height: 2.45rem; white-space: nowrap; }
-.admin-schedule-navigation { position: relative; z-index: 20; display: grid; grid-template-columns: minmax(0, 1fr) auto; align-items: center; gap: 0.5rem; padding: 0.55rem 1rem; border-bottom: 1px solid #daddE4; background: white; }
+.admin-schedule-navigation { position: relative; z-index: 20; display: grid; grid-template-columns: minmax(0, 1fr) auto; align-items: center; gap: 0.5rem; padding: 0.55rem 1rem; border-bottom: 1px solid var(--line); background: white; }
 .admin-timetable-modes { display: flex; gap: 0.2rem; }
-.admin-timetable-modes .btn { min-height: 2.5rem; gap: 0.45rem; padding: 0.45rem 0.7rem; border: 0; color: #424b59; background: transparent; text-decoration: none; }
-.admin-timetable-modes .btn[aria-current="page"] { color: #0753a6; background: #e7effa; }
+.admin-timetable-modes .btn { min-height: 2.5rem; gap: 0.45rem; padding: 0.45rem 0.7rem; border: 0; color: var(--ink); background: transparent; text-decoration: none; }
+.admin-timetable-modes .btn[aria-current="page"] { color: var(--brand); background: var(--surface-soft); }
 .admin-mode-label--desktop { display: none; }
 .admin-audience-selector { position: relative; }
-.admin-audience-selector summary { display: grid; min-width: 10rem; padding: 0.35rem 0.65rem; border: 1px solid #c7ced8; border-radius: 0.4rem; cursor: pointer; list-style: none; }
+.admin-audience-selector summary { display: grid; min-width: 10rem; padding: 0.35rem 0.65rem; border: 1px solid var(--line); border-radius: 0.4rem; cursor: pointer; list-style: none; }
 .admin-audience-selector summary::-webkit-details-marker { display: none; }
-.admin-audience-selector summary span { color: #687180; font-size: 0.62rem; letter-spacing: 0.08em; text-transform: uppercase; }
-.admin-audience-selector > div { position: absolute; z-index: 90; top: calc(100% + 0.35rem); right: 0; display: grid; width: min(20rem, 85vw); max-height: 24rem; overflow-y: auto; padding: 0.4rem; border: 1px solid #c7ced8; border-radius: 0.45rem; background: white; box-shadow: 0 0.8rem 2rem rgb(13 26 45 / 18%); }
-.admin-audience-selector a { padding: 0.6rem 0.7rem; border-radius: 0.3rem; color: #273344; text-decoration: none; }
+.admin-audience-selector summary span { color: var(--muted); font-size: 0.62rem; letter-spacing: 0.08em; text-transform: uppercase; }
+.admin-audience-selector > div { position: absolute; z-index: 90; top: calc(100% + 0.35rem); right: 0; display: grid; width: min(20rem, 85vw); max-height: 24rem; overflow-y: auto; padding: 0.4rem; border: 1px solid var(--line); border-radius: 0.45rem; background: white; box-shadow: 0 0.8rem 2rem rgb(38 24 22 / 18%); }
+.admin-audience-selector a { padding: 0.6rem 0.7rem; border-radius: 0.3rem; color: var(--ink); text-decoration: none; }
 .admin-audience-selector a:hover,
-.admin-audience-selector a[aria-current="page"] { color: #0753a6; background: #eaf1fa; }
+.admin-audience-selector a[aria-current="page"] { color: var(--brand); background: var(--surface-soft); }
 .admin-day-picker { grid-column: 1 / -1; display: flex; gap: 0.3rem; overflow-x: auto; }
 .admin-day-picker .btn { flex: 1; min-height: 2.4rem; border: 0; white-space: nowrap; }
-.admin-week-switcher { grid-column: 1 / -1; display: grid; grid-template-columns: 2.4rem minmax(0, 1fr) 2.4rem; align-items: center; border-top: 1px solid #edf0f4; }
+.admin-week-switcher { grid-column: 1 / -1; display: grid; grid-template-columns: 2.4rem minmax(0, 1fr) 2.4rem; align-items: center; border-top: 1px solid var(--line); }
 .admin-week-switcher .btn { min-width: 2.4rem; min-height: 2.4rem; padding: 0; border: 0; }
-.admin-week-switcher strong { color: #4d5868; font-size: 0.76rem; text-align: center; }
+.admin-week-switcher strong { color: var(--ink); font-size: 0.76rem; text-align: center; }
 .admin-school-picker { grid-column: 1 / -1; display: flex; gap: 0.2rem; }
 .admin-school-picker .btn { flex: 1; min-height: 2.35rem; border: 0; }
-.admin-school-picker .btn[aria-current="page"] { color: #0753a6; background: #e7effa; }
-.admin-workspace-hint { margin: 0; padding: 0.6rem 1rem; border-bottom: 1px solid #daddE4; color: #586474; background: #eef4fb; font-size: 0.76rem; }
+.admin-school-picker .btn[aria-current="page"] { color: var(--brand); background: var(--surface-soft); }
+.admin-workspace-hint { margin: 0; padding: 0.6rem 1rem; border-bottom: 1px solid var(--line); color: var(--muted); background: var(--surface-soft); font-size: 0.76rem; }
 .admin-timetable-desktop,
 .admin-school-desktop { display: none; }
 .admin-timetable-mobile,
@@ -67,29 +67,29 @@
 .admin-mobile-timeline { min-height: 24rem; padding: 0.8rem 1rem 6rem; }
 .admin-mobile-timeline ol { display: grid; gap: 0.7rem; margin: 0; padding: 0; list-style: none; }
 .admin-mobile-timeline li { display: grid; grid-template-columns: 3.7rem minmax(0, 1fr); gap: 0.8rem; align-items: start; }
-.admin-mobile-timeline li > time { display: grid; justify-items: end; padding-top: 0.55rem; color: #384454; font-size: 0.82rem; font-weight: 800; font-variant-numeric: tabular-nums; }
-.admin-mobile-timeline li > time span { color: #7a8492; font-size: 0.68rem; font-weight: 500; }
-.admin-empty-state { padding: 2rem; color: #687180; text-align: center; }
-.admin-occurrence-card { display: grid; width: 100%; min-width: 0; min-height: 3.8rem; gap: 0.2rem; padding: 0.55rem 0.7rem; overflow: hidden; border: 1px solid #cbd2dc; border-inline-start: 0.28rem solid var(--admin-entry-color, #0753a6); border-radius: 0.35rem; color: #202a38; background: white; box-shadow: 0 1px 2px rgb(22 29 40 / 7%); font: inherit; text-align: left; cursor: pointer; }
-.admin-occurrence-card:hover { border-color: #0753a6; box-shadow: 0 0.25rem 0.9rem rgb(7 83 166 / 12%); }
+.admin-mobile-timeline li > time { display: grid; justify-items: end; padding-top: 0.55rem; color: var(--ink); font-size: 0.82rem; font-weight: 800; font-variant-numeric: tabular-nums; }
+.admin-mobile-timeline li > time span { color: var(--muted); font-size: 0.68rem; font-weight: 500; }
+.admin-empty-state { padding: 2rem; color: var(--muted); text-align: center; }
+.admin-occurrence-card { display: grid; width: 100%; min-width: 0; min-height: 3.8rem; gap: 0.2rem; padding: 0.55rem 0.7rem; overflow: hidden; border: 1px solid var(--line); border-inline-start: 0.28rem solid var(--admin-entry-color, var(--brand)); border-radius: 0.35rem; color: var(--ink); background: white; box-shadow: 0 1px 2px rgb(38 24 22 / 7%); font: inherit; text-align: left; cursor: pointer; }
+.admin-occurrence-card:hover { border-color: var(--brand); box-shadow: 0 0.25rem 0.9rem rgb(184 24 27 / 12%); }
 .admin-occurrence-card--cancelled,
-.admin-occurrence-card--replaced { border-inline-start-color: #8a929e; background: repeating-linear-gradient(135deg, #f0f1f3 0, #f0f1f3 0.4rem, #fafafa 0.4rem, #fafafa 0.8rem); opacity: 0.78; }
+.admin-occurrence-card--replaced { border-inline-start-color: #8a929e; background: repeating-linear-gradient(135deg, var(--surface-soft) 0, var(--surface-soft) 0.4rem, #fafafa 0.4rem, #fafafa 0.8rem); opacity: 0.78; }
 .admin-occurrence-card__top { display: flex; min-width: 0; align-items: start; justify-content: space-between; gap: 0.4rem; }
 .admin-occurrence-card strong { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
 .admin-occurrence-card small { flex: 0 0 auto; padding: 0.12rem 0.3rem; border-radius: 999px; color: #765300; background: #fff0b8; font-size: 0.56rem; text-transform: uppercase; }
-.admin-occurrence-card > span:not(.admin-occurrence-card__top) { overflow: hidden; color: #657183; font-size: 0.68rem; text-overflow: ellipsis; white-space: nowrap; }
+.admin-occurrence-card > span:not(.admin-occurrence-card__top) { overflow: hidden; color: var(--muted); font-size: 0.68rem; text-overflow: ellipsis; white-space: nowrap; }
 .admin-school-grid { min-width: 0; padding: 0.8rem 1rem; }
 .admin-school-grid > h2 { margin: 0 0 0.55rem; font-size: 1rem; }
-.admin-school-grid__scroller { width: 100%; overflow-x: auto; border: 1px solid #cbd2dc; background: white; }
+.admin-school-grid__scroller { width: 100%; overflow-x: auto; border: 1px solid var(--line); background: white; }
 .admin-school-grid header,
 .admin-school-grid__row { display: grid; grid-template-columns: 4.8rem minmax(46rem, 1fr); min-width: 51rem; }
-.admin-school-grid header { min-height: 2.5rem; border-bottom: 1px solid #cbd2dc; background: #e9ecf0; }
+.admin-school-grid header { min-height: 2.5rem; border-bottom: 1px solid var(--line); background: var(--surface-soft); }
 .admin-school-grid header > span,
-.admin-school-grid__row > h3 { display: grid; margin: 0; place-items: center; border-right: 1px solid #cbd2dc; font-size: 0.72rem; }
+.admin-school-grid__row > h3 { display: grid; margin: 0; place-items: center; border-right: 1px solid var(--line); font-size: 0.72rem; }
 .admin-school-grid header > div,
 .admin-school-grid__track { position: relative; min-height: 4.2rem; }
-.admin-school-grid header time { position: absolute; left: calc((var(--admin-time) - var(--admin-grid-start)) / var(--admin-grid-duration) * 100%); top: 50%; transform: translate(-50%, -50%); color: #505a68; font-size: 0.62rem; }
-.admin-school-grid__row { border-bottom: 1px solid #d7dce3; }
+.admin-school-grid header time { position: absolute; left: calc((var(--admin-time) - var(--admin-grid-start)) / var(--admin-grid-duration) * 100%); top: 50%; transform: translate(-50%, -50%); color: var(--ink); font-size: 0.62rem; }
+.admin-school-grid__row { border-bottom: 1px solid var(--line); }
 .admin-school-grid__row:last-child { border-bottom: 0; }
 .admin-school-grid__entry { position: absolute; top: 0.22rem; bottom: 0.22rem; left: calc((var(--admin-entry-start) - var(--admin-grid-start)) / var(--admin-grid-duration) * 100% + 0.12rem); width: calc(var(--admin-entry-duration) / var(--admin-grid-duration) * 100% - 0.24rem); min-width: 2.5rem; }
 .admin-school-grid__break { position: absolute; z-index: 0; top: 0; bottom: 0; left: calc((var(--admin-entry-start) - var(--admin-grid-start)) / var(--admin-grid-duration) * 100%); width: calc(var(--admin-entry-duration) / var(--admin-grid-duration) * 100%); background: repeating-linear-gradient(135deg, #f1ead2 0, #f1ead2 0.28rem, #f8f4e7 0.28rem, #f8f4e7 0.56rem); }
@@ -101,53 +101,53 @@
 .admin-block-panel { position: relative; display: grid; width: min(100%, 34rem); max-height: 88vh; margin-inline: auto; padding: 1rem 1.1rem max(1.2rem, env(safe-area-inset-bottom)); overflow-y: auto; border-radius: 1rem 1rem 0 0; background: white; box-shadow: 0 -0.5rem 2rem rgb(0 0 0 / 18%); outline: none; }
 .admin-block-panel header { display: flex; align-items: start; justify-content: space-between; gap: 1rem; }
 .admin-block-panel h2 { margin: 0.1rem 0; }
-.admin-block-panel header p:not(.eyebrow) { margin: 0; color: #697484; font-size: 0.78rem; }
-.admin-block-panel .eyebrow { color: #0753a6; }
-.admin-block-panel__close { display: grid; width: 2.5rem; height: 2.5rem; place-items: center; border: 0; border-radius: 50%; color: #344052; background: #eef1f5; font-size: 1.5rem; cursor: pointer; }
+.admin-block-panel header p:not(.eyebrow) { margin: 0; color: var(--muted); font-size: 0.78rem; }
+.admin-block-panel .eyebrow { color: var(--brand); }
+.admin-block-panel__close { display: grid; width: 2.5rem; height: 2.5rem; place-items: center; border: 0; border-radius: 50%; color: var(--ink); background: var(--surface-soft); font-size: 1.5rem; cursor: pointer; }
 .admin-block-panel__actions { display: grid; gap: 0.35rem; margin-top: 1rem; }
-.admin-block-panel__actions button { display: grid; min-height: 3.4rem; gap: 0.15rem; padding: 0.65rem 0.75rem; border: 0; border-bottom: 1px solid #e1e5eb; color: #253143; background: transparent; font: inherit; text-align: left; cursor: pointer; }
+.admin-block-panel__actions button { display: grid; min-height: 3.4rem; gap: 0.15rem; padding: 0.65rem 0.75rem; border: 0; border-bottom: 1px solid var(--line); color: var(--ink); background: transparent; font: inherit; text-align: left; cursor: pointer; }
 .admin-block-panel__action { grid-template-columns: 1.3rem minmax(0, 1fr); align-items: start; column-gap: 0.6rem; }
 .admin-block-panel__action > svg { margin-top: 0.1rem; font-size: 1.1rem; }
 .admin-block-panel__action > span { display: grid; gap: 0.15rem; }
 .admin-block-panel__actions .admin-block-panel__action--danger { color: #b42318; }
 .admin-block-panel__actions button.admin-block-panel__action--danger:hover:not(:disabled) { color: #b42318; background: #fef0f0; }
-.admin-block-panel__actions button:hover:not(:disabled) { color: #0753a6; background: #eef4fb; }
+.admin-block-panel__actions button:hover:not(:disabled) { color: var(--brand); background: var(--surface-soft); }
 .admin-block-panel__actions button:disabled { color: #8a929e; cursor: not-allowed; }
 .admin-block-panel__actions span { font-size: 0.72rem; }
-.admin-block-panel__back { order: 10; width: fit-content; margin-top: 0.85rem; padding: 0.55rem 0; border: 0; color: #0753a6; background: transparent; font: inherit; font-weight: 750; cursor: pointer; }
+.admin-block-panel__back { order: 10; width: fit-content; margin-top: 0.85rem; padding: 0.55rem 0; border: 0; color: var(--brand); background: transparent; font: inherit; font-weight: 750; cursor: pointer; }
 .admin-add-close { position: absolute; z-index: 2; top: 0.8rem; right: 0.8rem; }
 .variation-editor-stack,
 .variation-editor { display: grid; gap: 0.85rem; }
 .variation-editor { margin-top: 1rem; }
-.variation-editor-stack > .variation-editor + .variation-editor { padding-top: 1rem; border-top: 1px solid #dce2e9; }
+.variation-editor-stack > .variation-editor + .variation-editor { padding-top: 1rem; border-top: 1px solid var(--line); }
 .variation-editor header h2,
 .variation-editor header h3,
 .variation-editor header p { margin: 0; }
 .admin-block-panel .variation-editor > header { display: block; }
 .variation-editor > label,
 .variation-form-grid label,
-.variation-conflict-confirmation > label { display: grid; gap: 0.3rem; color: #3f4a5a; font-size: 0.76rem; font-weight: 750; }
+.variation-conflict-confirmation > label { display: grid; gap: 0.3rem; color: var(--ink); font-size: 0.76rem; font-weight: 750; }
 .variation-editor input:not([type="checkbox"]):not([type="radio"]),
 .variation-editor select,
-.variation-editor textarea { width: 100%; min-height: 2.55rem; padding: 0.55rem 0.65rem; border: 1px solid #b9c3cf; border-radius: 0.38rem; color: #1d2838; background: white; font: inherit; }
+.variation-editor textarea { width: 100%; min-height: 2.55rem; padding: 0.55rem 0.65rem; border: 1px solid var(--line); border-radius: 0.38rem; color: #1d2838; background: white; font: inherit; }
 .variation-editor textarea { resize: vertical; }
 .variation-editor__hint,
-.variation-slot-list { margin: 0; color: #687486; font-size: 0.72rem; line-height: 1.45; }
+.variation-slot-list { margin: 0; color: var(--muted); font-size: 0.72rem; line-height: 1.45; }
 .variation-form-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 0.6rem; }
 .lesson-participants-form .variation-form-grid { grid-template-columns: minmax(8rem, 1.4fr) repeat(2, minmax(6rem, 1fr)); }
 .variation-category-picker,
-.variation-check-list { display: grid; gap: 0.35rem; margin: 0; padding: 0.75rem; border: 1px solid #d5dce5; border-radius: 0.5rem; }
+.variation-check-list { display: grid; gap: 0.35rem; margin: 0; padding: 0.75rem; border: 1px solid var(--line); border-radius: 0.5rem; }
 .variation-category-picker legend,
-.variation-check-list legend { padding-inline: 0.25rem; color: #3f4a5a; font-size: 0.76rem; font-weight: 800; }
+.variation-check-list legend { padding-inline: 0.25rem; color: var(--ink); font-size: 0.76rem; font-weight: 800; }
 .variation-category-option { display: grid; grid-template-columns: auto 1rem minmax(0, 1fr); align-items: center; gap: 0.5rem; min-height: 2rem; cursor: pointer; }
 .variation-category-option > span { width: 0.9rem; height: 0.9rem; border: 1px solid rgb(0 0 0 / 12%); border-radius: 0.2rem; }
-.variation-category-option--neutral > span { background: #eef1f5; }
+.variation-category-option--neutral > span { background: var(--surface-soft); }
 .variation-check-list { max-height: 12rem; overflow-y: auto; }
-.variation-check-list label { display: grid; grid-template-columns: auto minmax(0, 1fr); align-items: start; gap: 0.55rem; padding: 0.25rem; color: #394658; font-size: 0.76rem; cursor: pointer; }
+.variation-check-list label { display: grid; grid-template-columns: auto minmax(0, 1fr); align-items: start; gap: 0.55rem; padding: 0.25rem; color: var(--ink); font-size: 0.76rem; cursor: pointer; }
 .variation-category-picker input,
 .variation-check-list input,
-.variation-confirm input { width: 1.05rem; height: 1.05rem; accent-color: #0753a6; }
-.variation-confirm { display: grid; grid-template-columns: auto minmax(0, 1fr); align-items: start; gap: 0.55rem; color: #4a5564; font-size: 0.75rem; font-weight: 700; }
+.variation-confirm input { width: 1.05rem; height: 1.05rem; accent-color: var(--brand); }
+.variation-confirm { display: grid; grid-template-columns: auto minmax(0, 1fr); align-items: start; gap: 0.55rem; color: var(--ink); font-size: 0.75rem; font-weight: 700; }
 .variation-editor--danger { padding: 0.85rem; border: 1px solid #dfb4ba; border-radius: 0.55rem; background: #fff7f8; }
 .variation-feedback { margin: 0; padding: 0.7rem; border-radius: 0.4rem; font-size: 0.74rem; }
 .variation-feedback p { margin: 0.2rem 0 0; }
@@ -157,9 +157,9 @@
 .variation-conflict-confirmation h3,
 .variation-conflict-confirmation p { margin: 0; }
 .variation-conflict-confirmation p { color: #6c5a27; font-size: 0.7rem; }
-.variation-substitution-style { display: grid; gap: 0.7rem; margin-top: 1rem; padding-top: 1rem; border-top: 1px solid #dce2e9; }
+.variation-substitution-style { display: grid; gap: 0.7rem; margin-top: 1rem; padding-top: 1rem; border-top: 1px solid var(--line); }
 .substitution-panel { display: grid; gap: 1rem; margin-top: 1rem; }
-.substitution-panel__heading { display: flex; align-items: start; justify-content: space-between; gap: 0.7rem; padding-bottom: 0.7rem; border-bottom: 1px solid #dde2e9; }
+.substitution-panel__heading { display: flex; align-items: start; justify-content: space-between; gap: 0.7rem; padding-bottom: 0.7rem; border-bottom: 1px solid var(--line); }
 .substitution-panel__heading h3,
 .substitution-panel__heading p { margin: 0; }
 .substitution-step,
@@ -167,76 +167,76 @@
 .substitution-step label,
 .substitution-editor > label,
 .admin-guided-filter label,
-.extra-hours-report form label { display: grid; gap: 0.3rem; color: #3f4a5a; font-size: 0.76rem; font-weight: 750; }
+.extra-hours-report form label { display: grid; gap: 0.3rem; color: var(--ink); font-size: 0.76rem; font-weight: 750; }
 .substitution-step select,
 .substitution-step input,
 .substitution-step textarea,
 .substitution-editor textarea,
 .admin-guided-filter select,
-.extra-hours-report input { width: 100%; min-height: 2.65rem; padding: 0.55rem 0.65rem; border: 1px solid #bcc5d1; border-radius: 0.35rem; color: #1d2838; background: white; font: inherit; }
-.teacher-multi-select { border: 1px solid #d6dce4; border-radius: 0.45rem; background: #f8fafc; }
-.teacher-multi-select summary { padding: 0.6rem 0.7rem; color: #344154; cursor: pointer; font-size: 0.76rem; font-weight: 750; }
+.extra-hours-report input { width: 100%; min-height: 2.65rem; padding: 0.55rem 0.65rem; border: 1px solid var(--line); border-radius: 0.35rem; color: #1d2838; background: white; font: inherit; }
+.teacher-multi-select { border: 1px solid var(--line); border-radius: 0.45rem; background: var(--page); }
+.teacher-multi-select summary { padding: 0.6rem 0.7rem; color: var(--ink); cursor: pointer; font-size: 0.76rem; font-weight: 750; }
 .teacher-multi-select fieldset { display: grid; gap: 0.45rem; margin: 0; padding: 0 0.7rem 0.7rem; border: 0; }
-.teacher-multi-select legend { padding: 0; color: #3f4a5a; font-size: 0.76rem; font-weight: 750; }
-.teacher-multi-select label { display: flex; align-items: center; min-height: 1.125rem; gap: 0.5rem; color: #3f4a5a; cursor: pointer; font-size: 0.76rem; font-weight: 650; }
-.teacher-multi-select input[type="checkbox"] { box-sizing: border-box; flex: 0 0 1.125rem; width: 1.125rem; min-height: 1.125rem; height: 1.125rem; margin: 0; padding: 0; accent-color: #0753a6; }
+.teacher-multi-select legend { padding: 0; color: var(--ink); font-size: 0.76rem; font-weight: 750; }
+.teacher-multi-select label { display: flex; align-items: center; min-height: 1.125rem; gap: 0.5rem; color: var(--ink); cursor: pointer; font-size: 0.76rem; font-weight: 650; }
+.teacher-multi-select input[type="checkbox"] { box-sizing: border-box; flex: 0 0 1.125rem; width: 1.125rem; min-height: 1.125rem; height: 1.125rem; margin: 0; padding: 0; accent-color: var(--brand); }
 .candidate-tiers { display: grid; gap: 0.85rem; }
-.candidate-tier { display: grid; gap: 0.35rem; padding-top: 0.65rem; border-top: 2px solid #d9dfe7; }
-.candidate-tier h4 { margin: 0 0 0.25rem; color: #344154; font-size: 0.78rem; letter-spacing: 0.02em; }
+.candidate-tier { display: grid; gap: 0.35rem; padding-top: 0.65rem; border-top: 2px solid var(--line); }
+.candidate-tier h4 { margin: 0 0 0.25rem; color: var(--ink); font-size: 0.78rem; letter-spacing: 0.02em; }
 .candidate-tier--warning { border-top-color: #d89a28; }
 .candidate-tier--blocked { opacity: 0.72; }
-.candidate-option-wrap { display: grid; gap: 0.25rem; padding: 0.35rem 0.45rem; border: 1px solid #d6dce4; border-radius: 0.45rem; }
+.candidate-option-wrap { display: grid; gap: 0.25rem; padding: 0.35rem 0.45rem; border: 1px solid var(--line); border-radius: 0.45rem; }
 .candidate-option { display: grid; grid-template-columns: auto minmax(0, 1fr); align-items: start; gap: 0.55rem; cursor: pointer; }
-.candidate-option input { width: 1.1rem; height: 1.1rem; margin-top: 0.2rem; accent-color: #0753a6; }
+.candidate-option input { width: 1.1rem; height: 1.1rem; margin-top: 0.2rem; accent-color: var(--brand); }
 .candidate-option > span { display: grid; min-width: 0; }
 .candidate-option strong { overflow-wrap: anywhere; }
-.candidate-option small { color: #697484; font-size: 0.66rem; font-weight: 500; }
+.candidate-option small { color: var(--muted); font-size: 0.66rem; font-weight: 500; }
 .candidate-warning-icon { display: inline-grid; width: 1.2rem; height: 1.2rem; margin-left: 0.4rem; place-items: center; border-radius: 50%; color: white; background: #a85d00; font-size: 0.7rem; }
 .candidate-conflict { margin-left: 1.65rem; color: #754500; font-size: 0.7rem; }
 .candidate-conflict p { margin: 0.2rem 0; }
 .candidate-conflict-confirmation { display: grid; grid-template-columns: auto minmax(0, 1fr); gap: 0.5rem; margin-left: 1.65rem; color: #754500; font-size: 0.7rem; font-weight: 750; }
-.substitution-editor__controls { position: sticky; z-index: 5; bottom: 0; display: grid; gap: 0.7rem; padding: 0.75rem; border: 1px solid #d5dce5; border-radius: 0.6rem; background: rgb(255 255 255 / 98%); box-shadow: 0 -0.4rem 1rem rgb(22 35 54 / 10%); }
+.substitution-editor__controls { position: sticky; z-index: 5; bottom: 0; display: grid; gap: 0.7rem; padding: 0.75rem; border: 1px solid var(--line); border-radius: 0.6rem; background: rgb(255 255 255 / 98%); box-shadow: 0 -0.4rem 1rem rgb(38 24 22 / 10%); }
 .substitution-editor__actions { display: grid; grid-template-columns: minmax(0, 1fr) minmax(0, 1fr); gap: 0.55rem; }
 .substitution-disposition { display: grid; grid-template-columns: 1fr 1fr; gap: 0.55rem; margin: 0; padding: 0; border: 0; }
-.substitution-disposition legend { grid-column: 1 / -1; margin-bottom: 0.25rem; color: #3f4a5a; font-size: 0.76rem; font-weight: 750; }
-.substitution-disposition label { display: grid; grid-template-columns: auto minmax(0, 1fr); gap: 0.5rem; padding: 0.65rem; border: 1px solid #cbd2dc; border-radius: 0.45rem; cursor: pointer; }
+.substitution-disposition legend { grid-column: 1 / -1; margin-bottom: 0.25rem; color: var(--ink); font-size: 0.76rem; font-weight: 750; }
+.substitution-disposition label { display: grid; grid-template-columns: auto minmax(0, 1fr); gap: 0.5rem; padding: 0.65rem; border: 1px solid var(--line); border-radius: 0.45rem; cursor: pointer; }
 .substitution-disposition label:first-of-type { background: #fff7cf; }
 .substitution-disposition label:last-of-type { background: #fde6e6; }
 .substitution-disposition span { display: grid; }
 .substitution-disposition small { font-size: 0.63rem; font-weight: 500; }
 .substitution-error { padding: 0.7rem; border-left: 0.28rem solid #a51d2d; color: #6e1823; background: #fff0f2; }
 .substitution-error p { margin: 0.25rem 0 0; font-size: 0.72rem; }
-.substitution-cancel { padding-top: 0.8rem; border-top: 1px solid #dde2e9; }
+.substitution-cancel { padding-top: 0.8rem; border-top: 1px solid var(--line); }
 .button--danger { color: #8e1f2d; border-color: #d9a7ae; background: #fff3f4; }
 .admin-substitutions-page { display: grid; gap: 1.25rem; max-width: 72rem; }
 .admin-guided-card,
-.extra-hours-report { display: grid; gap: 1rem; padding: clamp(1rem, 3vw, 1.5rem); border: 1px solid #d6dde7; border-radius: 0.75rem; background: white; }
+.extra-hours-report { display: grid; gap: 1rem; padding: clamp(1rem, 3vw, 1.5rem); border: 1px solid var(--line); border-radius: 0.75rem; background: white; }
 .admin-guided-card h2,
 .extra-hours-report h2,
 .admin-guided-card p,
 .extra-hours-report p { margin: 0.15rem 0; }
 .admin-guided-filter { display: grid; grid-template-columns: 1fr 1fr auto; align-items: end; gap: 0.7rem; }
 .absence-management__form { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); align-items: end; gap: 0.7rem; }
-.absence-management__form label { display: grid; gap: 0.3rem; color: #3f4a5a; font-size: 0.76rem; font-weight: 750; }
+.absence-management__form label { display: grid; gap: 0.3rem; color: var(--ink); font-size: 0.76rem; font-weight: 750; }
 .absence-management__form input,
 .absence-management__form select,
-.absence-management__form textarea { width: 100%; min-height: 2.65rem; padding: 0.55rem 0.65rem; border: 1px solid #bcc5d1; border-radius: 0.35rem; color: #1d2838; background: white; font: inherit; }
+.absence-management__form textarea { width: 100%; min-height: 2.65rem; padding: 0.55rem 0.65rem; border: 1px solid var(--line); border-radius: 0.35rem; color: #1d2838; background: white; font: inherit; }
 .absence-management__reason { grid-column: 1 / -1; }
 .absence-management__form .button { width: fit-content; }
-.absence-management__list { display: grid; gap: 0.55rem; padding-top: 0.85rem; border-top: 1px solid #d6dde7; }
+.absence-management__list { display: grid; gap: 0.55rem; padding-top: 0.85rem; border-top: 1px solid var(--line); }
 .absence-management__list h3 { margin: 0; font-size: 0.95rem; }
 .absence-management__list ul { display: grid; gap: 0.45rem; margin: 0; padding: 0; list-style: none; }
-.absence-management__list li { display: grid; gap: 0.15rem; padding: 0.65rem; border: 1px solid #dce2e9; border-radius: 0.45rem; color: #3f4a5a; background: #f8fafc; font-size: 0.76rem; }
-.absence-management__list li[aria-current="true"] { border-color: #7aa4d4; background: #eef6ff; }
-.absence-management__list small { color: #687180; }
+.absence-management__list li { display: grid; gap: 0.15rem; padding: 0.65rem; border: 1px solid var(--line); border-radius: 0.45rem; color: var(--ink); background: var(--page); font-size: 0.76rem; }
+.absence-management__list li[aria-current="true"] { border-color: var(--brand); background: var(--surface-soft); }
+.absence-management__list small { color: var(--muted); }
 .admin-guided-card > .button { width: fit-content; text-decoration: none; }
-.admin-guided-card__hint { color: #687180; font-size: 0.78rem; }
+.admin-guided-card__hint { color: var(--muted); font-size: 0.78rem; }
 .extra-hours-report header { display: flex; align-items: end; justify-content: space-between; gap: 1rem; }
 .extra-hours-report form { display: flex; align-items: end; gap: 0.5rem; }
 .extra-hours-report__table-wrap { overflow-x: auto; }
 .extra-hours-report table { width: 100%; border-collapse: collapse; }
 .extra-hours-report th,
-.extra-hours-report td { padding: 0.65rem; border-bottom: 1px solid #e1e5eb; text-align: left; }
+.extra-hours-report td { padding: 0.65rem; border-bottom: 1px solid var(--line); text-align: left; }
 .extra-hours-report td:last-child,
 .extra-hours-report thead th:last-child { text-align: right; }
 .admin-status-badge { width: fit-content; padding: 0.25rem 0.45rem; border-radius: 999px; color: #5b2530; background: #fae0e4; font-size: 0.68rem; font-weight: 800; }
@@ -247,8 +247,8 @@
 @media (max-width: 59.999rem) {
   .admin-app { padding-bottom: 4.25rem; }
   .admin-nav { display: none; }
-  .admin-bottom-nav { position: fixed; z-index: 70; right: 0; bottom: 0; left: 0; display: grid; grid-template-columns: repeat(5, minmax(0, 1fr)); min-height: 4.25rem; padding-bottom: env(safe-area-inset-bottom); border-top: 1px solid #c8cfda; background: rgb(255 255 255 / 98%); }
-  .admin-bottom-nav a { display: grid; place-items: center; padding: 0.35rem 0.2rem; overflow: hidden; color: #4d5665; font-size: 0.62rem; font-weight: 750; text-align: center; text-decoration: none; }
+  .admin-bottom-nav { position: fixed; z-index: 70; right: 0; bottom: 0; left: 0; display: grid; grid-template-columns: repeat(5, minmax(0, 1fr)); min-height: 4.25rem; padding-bottom: env(safe-area-inset-bottom); border-top: 1px solid var(--line); background: rgb(255 255 255 / 98%); }
+  .admin-bottom-nav a { display: grid; place-items: center; padding: 0.35rem 0.2rem; overflow: hidden; color: var(--ink); font-size: 0.62rem; font-weight: 750; text-align: center; text-decoration: none; }
   .admin-header { padding: 0.65rem 1rem; }
   .admin-header p,
   .admin-account > span { display: none; }
@@ -289,15 +289,15 @@
   .admin-school-desktop { display: block; }
   .admin-school-desktop { padding: 0.8rem 1.8rem 2rem; }
   .admin-school-desktop .admin-school-grid { padding: 0 0 1.5rem; }
-  .admin-desktop-week { margin: 0.8rem 1.8rem 2rem; overflow: auto; border: 1px solid #cbd2dc; background: white; }
-  .admin-desktop-week > header { display: grid; grid-template-columns: 4rem repeat(5, minmax(9rem, 1fr)); min-width: 49rem; min-height: 2.5rem; border-bottom: 1px solid #cbd2dc; background: #e9ecf0; }
+  .admin-desktop-week { margin: 0.8rem 1.8rem 2rem; overflow: auto; border: 1px solid var(--line); background: white; }
+  .admin-desktop-week > header { display: grid; grid-template-columns: 4rem repeat(5, minmax(9rem, 1fr)); min-width: 49rem; min-height: 2.5rem; border-bottom: 1px solid var(--line); background: var(--surface-soft); }
   .admin-desktop-week > header > * { display: grid; place-items: center; font-size: 0.72rem; text-transform: capitalize; }
   .admin-desktop-week__body { position: relative; display: grid; grid-template-columns: 4rem minmax(45rem, 1fr); min-width: 49rem; min-height: calc(var(--admin-grid-duration) * 1.05px); }
-  .admin-desktop-week__axis { position: relative; margin: 0; padding: 0; border-right: 1px solid #cbd2dc; list-style: none; }
-  .admin-desktop-week__axis li { position: absolute; right: 0.4rem; top: calc((var(--admin-time) - var(--admin-grid-start)) / var(--admin-grid-duration) * 100%); transform: translateY(-50%); color: #647080; font-size: 0.66rem; }
+  .admin-desktop-week__axis { position: relative; margin: 0; padding: 0; border-right: 1px solid var(--line); list-style: none; }
+  .admin-desktop-week__axis li { position: absolute; right: 0.4rem; top: calc((var(--admin-time) - var(--admin-grid-start)) / var(--admin-grid-duration) * 100%); transform: translateY(-50%); color: var(--muted); font-size: 0.66rem; }
   .admin-desktop-week__body > section:first-of-type { grid-column: 2; }
   .admin-desktop-week__body { grid-template-columns: 4rem repeat(5, minmax(9rem, 1fr)); }
-  .admin-desktop-week__day { position: relative; min-height: calc(var(--admin-grid-duration) * 1.05px); border-right: 1px solid #dce1e7; background-image: repeating-linear-gradient(to bottom, transparent 0, transparent calc(60 / var(--admin-grid-duration) * 100% - 1px), #e5e8ed calc(60 / var(--admin-grid-duration) * 100% - 1px), #e5e8ed calc(60 / var(--admin-grid-duration) * 100%)); }
+  .admin-desktop-week__day { position: relative; min-height: calc(var(--admin-grid-duration) * 1.05px); border-right: 1px solid var(--line); background-image: repeating-linear-gradient(to bottom, transparent 0, transparent calc(60 / var(--admin-grid-duration) * 100% - 1px), var(--line) calc(60 / var(--admin-grid-duration) * 100% - 1px), var(--line) calc(60 / var(--admin-grid-duration) * 100%)); }
   .admin-desktop-week__break { position: absolute; z-index: 0; right: 0; left: 0; top: calc((var(--admin-entry-start) - var(--admin-grid-start)) / var(--admin-grid-duration) * 100%); height: calc(var(--admin-entry-duration) / var(--admin-grid-duration) * 100%); display: grid; place-items: center; color: #725b1c; background: repeating-linear-gradient(135deg, #f1ead2 0, #f1ead2 0.28rem, #f8f4e7 0.28rem, #f8f4e7 0.56rem); font-size: 0.6rem; font-weight: 750; }
   .admin-desktop-week__entry { position: absolute; right: 0.22rem; left: 0.22rem; top: calc((var(--admin-entry-start) - var(--admin-grid-start)) / var(--admin-grid-duration) * 100%); height: calc(var(--admin-entry-duration) / var(--admin-grid-duration) * 100%); padding: 0.12rem 0; }
   .admin-desktop-week__entry { z-index: 1; }
@@ -329,6 +329,67 @@ a {
   text-underline-offset: 0.2em;
 }
 
+:where(a, button, input, select, textarea, summary):focus-visible {
+  outline: 0.2rem solid var(--focus);
+  outline-offset: 0.15rem;
+}
+
+:where(input:not([type="checkbox"]):not([type="radio"]), select, textarea) {
+  border-color: var(--line-strong) !important;
+  color: var(--ink);
+  background-color: var(--surface);
+}
+
+:where(input[type="checkbox"], input[type="radio"]) {
+  accent-color: var(--brand);
+}
+
+.portal-brand {
+  display: inline-flex;
+  min-width: 0;
+  align-items: center;
+  gap: 0.6rem;
+  color: var(--ink);
+  text-decoration: none;
+}
+
+.portal-brand__logo {
+  display: block;
+  flex: 0 0 auto;
+  width: auto;
+  object-fit: contain;
+}
+
+.portal-brand__logo--full {
+  display: none;
+  height: 3rem;
+}
+
+.portal-brand__logo--mark {
+  height: 2.45rem;
+}
+
+.portal-brand__service {
+  padding-left: 0.6rem;
+  border-left: 1px solid var(--line);
+  color: var(--brand);
+  font-size: 0.82rem;
+  font-weight: 850;
+  letter-spacing: 0.04em;
+  line-height: 1;
+  text-transform: uppercase;
+}
+
+@media (min-width: 48rem) {
+  .portal-brand__logo--full {
+    display: block;
+  }
+
+  .portal-brand__logo--mark {
+    display: none;
+  }
+}
+
 .public-page {
   display: grid;
   gap: 1.5rem;
@@ -355,7 +416,7 @@ a {
   border: 1px solid var(--line);
   border-radius: 1rem;
   background: rgb(255 255 255 / 94%);
-  box-shadow: 0 0.65rem 2rem rgb(23 36 61 / 8%);
+  box-shadow: 0 0.65rem 2rem rgb(38 24 22 / 8%);
 }
 
 .schedule-controls .btn {
@@ -431,7 +492,7 @@ a {
   border: 1px solid var(--line);
   border-radius: 0.8rem;
   background: var(--paper);
-  box-shadow: 0 1rem 2rem rgb(23 36 61 / 16%);
+  box-shadow: 0 1rem 2rem rgb(38 24 22 / 16%);
 }
 
 .selector-menu .btn {
@@ -486,7 +547,7 @@ a {
   border-radius: 0.55rem;
   color: var(--ink);
   background: var(--paper);
-  box-shadow: 0 0.5rem 1.2rem rgb(23 36 61 / 14%);
+  box-shadow: 0 0.5rem 1.2rem rgb(38 24 22 / 14%);
   font-size: 0.78rem;
 }
 
@@ -528,7 +589,7 @@ a {
 .publication-meta {
   max-width: 24rem;
   margin: 0;
-  color: #dce9f8;
+  color: var(--surface-soft);
   text-align: right;
 }
 
@@ -537,7 +598,7 @@ a {
   border-radius: 1rem;
   overflow: hidden;
   background: var(--paper);
-  box-shadow: 0 0.65rem 2rem rgb(23 36 61 / 8%);
+  box-shadow: 0 0.65rem 2rem rgb(38 24 22 / 8%);
 }
 
 .week-grid {
@@ -812,6 +873,7 @@ a {
   background: var(--paper);
 }
 
+/* Schedule-entry colors below encode publication data/state, not product identity. */
 .schedule-card--substitution {
   box-shadow: inset 0 0 0 2px rgb(219 165 30 / 45%);
 }
@@ -1079,7 +1141,7 @@ a {
     padding: 0.55rem max(0.75rem, env(safe-area-inset-right)) max(0.55rem, env(safe-area-inset-bottom)) max(0.75rem, env(safe-area-inset-left));
     border-top: 1px solid var(--line);
     background: rgb(255 255 255 / 97%);
-    box-shadow: 0 -0.5rem 1.5rem rgb(23 36 61 / 10%);
+    box-shadow: 0 -0.5rem 1.5rem rgb(38 24 22 / 10%);
   }
 
   .mobile-bottom-nav .btn {
@@ -1136,7 +1198,7 @@ a {
   border: 1px solid var(--line);
   border-radius: 1rem;
   background: var(--paper);
-  box-shadow: 0 0.65rem 2rem rgb(23 36 61 / 8%);
+  box-shadow: 0 0.65rem 2rem rgb(38 24 22 / 8%);
   text-align: center;
 }
 
@@ -1161,7 +1223,7 @@ a {
   border: 1px solid var(--line);
   border-radius: 1rem;
   background: var(--paper);
-  box-shadow: 0 0.65rem 2rem rgb(23 36 61 / 8%);
+  box-shadow: 0 0.65rem 2rem rgb(38 24 22 / 8%);
 }
 
 .legal-document header {
@@ -1169,6 +1231,16 @@ a {
   border-bottom: 0.2rem solid var(--accent);
 }
 
+.legal-brand {
+  width: fit-content;
+  margin-bottom: 1.5rem;
+}
+
+.legal-brand .portal-brand__logo--full,
+.login-card__brand .portal-brand__logo--full {
+  height: 3.4rem;
+}
+
 .legal-document section {
   margin-top: 2rem;
 }
@@ -1257,7 +1329,7 @@ a {
   border: 1px solid var(--line);
   border-radius: 1.25rem;
   background: rgb(255 255 255 / 96%);
-  box-shadow: 0 1rem 3rem rgb(23 36 61 / 12%);
+  box-shadow: 0 1rem 3rem rgb(38 24 22 / 12%);
 }
 
 .home-hero h1,
@@ -1288,16 +1360,9 @@ a {
 }
 
 .login-card__brand {
-  display: grid;
-  width: 3rem;
-  height: 3rem;
+  width: fit-content;
+  height: auto;
   margin-bottom: 1.25rem;
-  place-items: center;
-  border-radius: 0.8rem;
-  color: white;
-  background: linear-gradient(135deg, var(--brand-dark), var(--brand));
-  font-size: 1.4rem;
-  font-weight: 900;
 }
 
 .login-action {
@@ -1310,7 +1375,7 @@ a {
   width: 100%;
   color: #26354d;
   background: white;
-  border-color: #b8c4d4;
+  border-color: var(--line);
 }
 
 .google-mark {
@@ -1343,20 +1408,27 @@ a {
   justify-content: space-between;
   gap: 1rem;
   padding: 1rem clamp(1rem, 3vw, 2.5rem);
-  color: white;
-  background: var(--brand-dark);
+  border-bottom: 1px solid var(--line);
+  color: var(--ink);
+  background: rgb(255 255 255 / 97%);
+  box-shadow: var(--shadow-sm);
 }
 
 .admin-brand {
-  color: white;
-  font-size: 1.2rem;
-  font-weight: 900;
-  text-decoration: none;
+  color: var(--ink);
+}
+
+.admin-identity {
+  display: flex;
+  align-items: center;
+  gap: 0.85rem;
 }
 
 .admin-header p {
-  margin: 0.2rem 0 0;
-  color: #dce9f8;
+  margin: 0;
+  padding-left: 0.85rem;
+  border-left: 1px solid var(--line);
+  color: var(--muted);
   font-size: 0.8rem;
 }
 
@@ -1370,17 +1442,17 @@ a {
 .logout-action button {
   min-height: 2.5rem;
   padding: 0.5rem 0.8rem;
-  border: 1px solid rgb(255 255 255 / 42%);
+  border: 1px solid var(--line-strong);
   border-radius: 0.65rem;
-  color: white;
-  background: transparent;
+  color: var(--brand-dark);
+  background: var(--surface);
   font: inherit;
   font-weight: 800;
   cursor: pointer;
 }
 
 .logout-action [role="alert"] {
-  color: #ffd7d7;
+  color: var(--danger);
   font-size: 0.78rem;
 }
 
@@ -1407,12 +1479,12 @@ a {
 }
 
 .admin-nav a:hover {
-  background: #e8f0fa;
+  background: var(--surface-soft);
 }
 
 .admin-nav a[aria-current="page"] {
-  color: #0753a6;
-  background: #e7effa;
+  color: var(--brand);
+  background: var(--surface-soft);
 }
 
 .admin-content > main {
@@ -1437,7 +1509,7 @@ a {
 }
 
 .admin-dashboard__intro > p:not(.eyebrow) {
-  color: #dce9f8;
+  color: var(--surface-soft);
 }
 
 .admin-action-grid {
@@ -1456,7 +1528,7 @@ a {
   border-radius: 0.9rem;
   color: var(--ink);
   background: var(--paper);
-  box-shadow: 0 0.5rem 1.5rem rgb(23 36 61 / 7%);
+  box-shadow: 0 0.5rem 1.5rem rgb(38 24 22 / 7%);
   text-decoration: none;
 }
 
@@ -1478,7 +1550,7 @@ a {
   padding: 1rem;
   border: 1px solid var(--line);
   border-radius: 0.8rem;
-  background: #edf3fa;
+  background: var(--surface-soft);
 }
 
 .admin-weeks-page {
@@ -1521,7 +1593,7 @@ a {
   align-content: start;
   gap: 1rem;
   padding: clamp(1.25rem, 4vw, 2rem);
-  box-shadow: 0 0.75rem 2rem rgb(23 36 61 / 7%);
+  box-shadow: 0 0.75rem 2rem rgb(38 24 22 / 7%);
 }
 
 .admin-weeks-calendar h2 {
@@ -1552,7 +1624,7 @@ a {
   padding: 0.75rem;
   border: 1px solid var(--line);
   border-radius: 0.7rem;
-  background: #f8fbff;
+  background: var(--page);
 }
 
 .week-date-picker__label,
@@ -1572,13 +1644,13 @@ a {
 
 .week-date-picker .rdp-root {
   --rdp-accent-color: var(--brand);
-  --rdp-accent-background-color: #dbeafa;
+  --rdp-accent-background-color: var(--surface-soft);
   margin: 0;
 }
 
 .week-date-picker .week-date-picker__day--week:not(.rdp-selected) button {
   border-radius: 0;
-  background: #eaf2fb;
+  background: var(--surface-soft);
 }
 
 .week-date-picker .rdp-day_button {
@@ -1591,7 +1663,7 @@ a {
   flex-direction: column;
   gap: 0.85rem;
   padding: clamp(1.25rem, 4vw, 2rem);
-  box-shadow: 0 0.75rem 2rem rgb(23 36 61 / 7%);
+  box-shadow: 0 0.75rem 2rem rgb(38 24 22 / 7%);
 }
 
 .admin-weeks-overview .week-bootstrap-card {
@@ -1623,7 +1695,7 @@ a {
   padding: 1rem;
   border-left: 0.3rem solid var(--brand);
   border-radius: 0.6rem;
-  background: #edf3fa;
+  background: var(--surface-soft);
 }
 
 .week-bootstrap-provenance span,
@@ -1633,7 +1705,7 @@ a {
 
 .admin-safety-note {
   padding: 1rem 1.2rem;
-  background: #edf3fa;
+  background: var(--surface-soft);
 }
 
 .admin-safety-note p {
@@ -1770,7 +1842,7 @@ a {
   max-width: none;
   padding: 0 0 4.25rem;
   gap: 0;
-  background: #f8f9fb;
+  background: var(--page);
   overflow-x: clip;
 }
 
@@ -1784,8 +1856,8 @@ a {
 .public-schedule-shell {
   display: block;
   min-height: 100vh;
-  color: #17191d;
-  background: #f8f9fb;
+  color: var(--ink);
+  background: var(--page);
 }
 
 .public-app-bar {
@@ -1797,16 +1869,12 @@ a {
   align-items: center;
   min-height: 4rem;
   padding: 0 1rem;
-  border-bottom: 1px solid #cfd5df;
-  background: rgb(248 249 251 / 96%);
+  border-bottom: 1px solid var(--line);
+  background: rgb(255 248 247 / 96%);
 }
 
 .public-brand {
-  color: #0753a6;
-  font-size: clamp(1.25rem, 4.8vw, 1.55rem);
-  font-weight: 800;
-  letter-spacing: -0.035em;
-  text-decoration: none;
+  width: fit-content;
   white-space: nowrap;
 }
 
@@ -1820,7 +1888,7 @@ a {
   grid-template-columns: minmax(0, 1fr) auto;
   min-width: 7.8rem;
   padding: 0.35rem 0.65rem;
-  border: 1px solid #c7cfdb;
+  border: 1px solid var(--line);
   border-radius: 0.35rem;
   background: white;
   cursor: pointer;
@@ -1829,7 +1897,7 @@ a {
 .contextual-selector summary::-webkit-details-marker { display: none; }
 .contextual-selector summary small {
   grid-column: 1;
-  color: #687180;
+  color: var(--muted);
   font-size: 0.62rem;
   letter-spacing: 0.08em;
   text-transform: uppercase;
@@ -1844,7 +1912,7 @@ a {
 }
 .contextual-selector summary > span { grid-column: 2; grid-row: 1 / 3; align-self: center; }
 .contextual-selector .selector-menu { position: absolute; z-index: 70; inset: calc(100% + 0.35rem) 0 auto auto; width: max-content; min-width: 13rem; max-width: calc(100vw - 2rem); }
-.portal-access { min-width: 2.65rem; min-height: 2.65rem; gap: 0.45rem; padding: 0.5rem; border-color: #0e315b; color: #fff; background: #0e315b; text-decoration: none; }
+.portal-access { min-width: 2.65rem; min-height: 2.65rem; gap: 0.45rem; padding: 0.5rem; border-color: var(--brand); color: #fff; background: var(--brand); text-decoration: none; }
 .portal-access:hover { color: #fff; background: var(--brand-hover); }
 .portal-access:focus-visible { color: #fff; background: var(--brand-hover); outline: 0.2rem solid var(--focus); outline-offset: 0.15rem; }
 .portal-access > span { display: none; }
@@ -1857,9 +1925,9 @@ a {
   gap: 0.25rem;
   margin: 0 -1rem;
   padding: 0.65rem 1rem;
-  border-top: 1px solid #e2e5ea;
-  border-bottom: 1px solid #daddE4;
-  background: #f2f3f5;
+  border-top: 1px solid var(--line);
+  border-bottom: 1px solid var(--line);
+  background: var(--surface-soft);
 }
 .date-navigation > .btn {
   min-width: 2.6rem;
@@ -1872,10 +1940,10 @@ a {
 .date-navigation > div,
 .published-date-selector > summary { display: grid; justify-items: center; min-width: 0; text-align: center; }
 .date-navigation > .desktop-export-actions { display: none; }
-.date-navigation small { color: #687180; font-size: 0.65rem; letter-spacing: 0.08em; text-transform: uppercase; }
+.date-navigation small { color: var(--muted); font-size: 0.65rem; letter-spacing: 0.08em; text-transform: uppercase; }
 .date-navigation strong { overflow: hidden; font-size: 0.95rem; text-transform: capitalize; text-overflow: ellipsis; white-space: nowrap; }
 .date-navigation div > span,
-.published-date-selector summary > span { color: #555f6f; font-size: 0.76rem; text-transform: capitalize; }
+.published-date-selector summary > span { color: var(--muted); font-size: 0.76rem; text-transform: capitalize; }
 
 .published-date-selector { position: relative; min-width: 0; }
 .published-date-selector > summary { grid-template-columns: minmax(0, 1fr) auto; column-gap: 0.35rem; border-radius: 0.45rem; cursor: pointer; list-style: none; }
@@ -1883,10 +1951,10 @@ a {
 .published-date-selector > summary small,
 .published-date-selector > summary strong,
 .published-date-selector > summary span { grid-column: 1; }
-.published-date-selector > summary > svg { grid-column: 2; grid-row: 1 / 4; align-self: center; color: #0753a6; }
+.published-date-selector > summary > svg { grid-column: 2; grid-row: 1 / 4; align-self: center; color: var(--brand); }
 .published-date-selector > summary:hover { background: var(--surface-soft); }
 .published-date-selector > summary:focus-visible { background: var(--surface-soft); outline: 0.2rem solid var(--focus); outline-offset: 0.15rem; }
-.published-date-selector__calendar { position: absolute; z-index: 75; top: calc(100% + 0.45rem); left: 50%; width: min(20rem, calc(100vw - 2rem)); transform: translateX(-50%); box-shadow: 0 0.75rem 2rem rgb(23 36 61 / 18%); }
+.published-date-selector__calendar { position: absolute; z-index: 75; top: calc(100% + 0.45rem); left: 50%; width: min(20rem, calc(100vw - 2rem)); transform: translateX(-50%); box-shadow: 0 0.75rem 2rem rgb(38 24 22 / 18%); }
 .published-date-selector__day--available:not(.rdp-disabled) button { font-weight: 800; }
 
 .share-menu { position: relative; }
@@ -1897,7 +1965,7 @@ a {
   place-items: center;
   border-radius: 0.55rem;
   color: white;
-  background: #0753a6;
+  background: var(--brand);
   font-size: 1.35rem;
   cursor: pointer;
   list-style: none;
@@ -1918,7 +1986,7 @@ a {
   background: white;
   box-shadow: 0 -0.5rem 2rem rgb(0 0 0 / 16%);
 }
-.share-sheet__panel > i { width: 3rem; height: 0.32rem; margin: 0 auto 0.65rem; border-radius: 999px; background: #d4d8e0; }
+.share-sheet__panel > i { width: 3rem; height: 0.32rem; margin: 0 auto 0.65rem; border-radius: 999px; background: var(--line); }
 .share-sheet h2 { margin: 0 0 0.6rem; font-size: 1.3rem; }
 .share-sheet a,
 .share-sheet button:not(.share-sheet__backdrop) {
@@ -1928,7 +1996,7 @@ a {
   gap: 1rem;
   padding: 0.7rem 0.55rem;
   border: 0;
-  color: #17191d;
+  color: var(--ink);
   background: transparent;
   font: inherit;
   text-align: left;
@@ -1936,9 +2004,9 @@ a {
   cursor: pointer;
 }
 .share-sheet a > svg,
-.share-sheet button > svg { width: 1.5rem; color: #0753a6; font-size: 1.25rem; text-align: center; }
-.share-sheet .share-sheet__cancel { justify-content: center; margin-top: 0.4rem; color: #0753a6; font-size: 0.78rem; font-weight: 800; text-transform: uppercase; }
-.share-schedule__status { color: #36526f; font-size: 0.78rem; text-align: center; }
+.share-sheet button > svg { width: 1.5rem; color: var(--brand); font-size: 1.25rem; text-align: center; }
+.share-sheet .share-sheet__cancel { justify-content: center; margin-top: 0.4rem; color: var(--brand); font-size: 0.78rem; font-weight: 800; text-transform: uppercase; }
+.share-schedule__status { color: var(--muted); font-size: 0.78rem; text-align: center; }
 
 .day-picker {
   grid-column: 1 / -1;
@@ -1947,7 +2015,7 @@ a {
   gap: 0.25rem;
   margin: 0 -1rem;
   padding: 0.45rem 1rem 0.55rem;
-  background: #f8f9fb;
+  background: var(--page);
 }
 .day-picker .btn {
   display: grid;
@@ -1958,13 +2026,13 @@ a {
   padding: 0.3rem;
   border: 0;
   border-radius: 0.35rem;
-  color: #474f5d;
+  color: var(--ink);
   background: transparent;
   font-size: 0.7rem;
   text-transform: uppercase;
 }
 .day-picker .btn strong { font-size: 1rem; }
-.day-picker .btn[aria-current="date"] { color: white; background: #0753a6; }
+.day-picker .btn[aria-current="date"] { color: white; background: var(--brand); }
 .public-schedule-shell--all .day-picker { display: none; }
 
 .schedule-heading {
@@ -1972,27 +2040,27 @@ a {
   padding: 0;
   border: 0;
   border-radius: 0;
-  color: #17191d;
+  color: var(--ink);
   background: transparent;
 }
 .school-selector {
   display: flex;
   gap: 0.2rem;
   padding: 0.5rem 1rem;
-  border-bottom: 1px solid #daddE4;
+  border-bottom: 1px solid var(--line);
   background: white;
 }
 .school-selector a {
   flex: 1;
   padding: 0.55rem 0.4rem;
   border-radius: 0.35rem;
-  color: #4d5563;
+  color: var(--ink);
   font-size: 0.84rem;
   font-weight: 750;
   text-align: center;
   text-decoration: none;
 }
-.school-selector a[aria-current="page"] { color: #0753a6; background: #eef2f7; }
+.school-selector a[aria-current="page"] { color: var(--brand); background: var(--surface-soft); }
 
 .published-schedule { border: 0; border-radius: 0; overflow: visible; background: transparent; box-shadow: none; }
 .desktop-week-grid,
@@ -2004,42 +2072,42 @@ a {
 .mobile-timeline {
   gap: 0.7rem;
   padding: 0.8rem 1rem 1.2rem;
-  background: #f8f9fb;
+  background: var(--page);
 }
-.mobile-timeline::before { top: 1.25rem; bottom: 1.25rem; left: 5.1rem; width: 1px; background: #cbd2dc; }
+.mobile-timeline::before { top: 1.25rem; bottom: 1.25rem; left: 5.1rem; width: 1px; background: var(--line); }
 .mobile-timeline__entry { display: grid; grid-template-columns: 4rem minmax(0, 1fr); gap: 1.4rem; padding: 0; }
 .mobile-timeline__entry::before { top: 1rem; left: 4.7rem; width: 0.6rem; height: 0.6rem; border-width: 0.15rem; }
-.mobile-timeline__time { display: grid; align-content: start; justify-items: end; padding-top: 0.45rem; color: #485262; font-variant-numeric: tabular-nums; }
+.mobile-timeline__time { display: grid; align-content: start; justify-items: end; padding-top: 0.45rem; color: var(--ink); font-variant-numeric: tabular-nums; }
 .mobile-timeline__time time { font-size: 0.9rem; font-weight: 800; }
-.mobile-timeline__time span { color: #77808e; font-size: 0.72rem; }
+.mobile-timeline__time span { color: var(--muted); font-size: 0.72rem; }
 .mobile-timeline__break { display: grid; grid-template-columns: 4rem minmax(0, 1fr); gap: 1.4rem; min-height: 2rem; padding: 0; }
 .mobile-timeline__break::before { top: 0.55rem; left: 4.75rem; }
 .mobile-timeline__break time { text-align: right; }
-.mobile-timeline__break span { align-self: center; border-block: 1px dashed #c5ccd7; text-align: center; }
+.mobile-timeline__break span { align-self: center; border-block: 1px dashed var(--line); text-align: center; }
 
 .schedule-card {
   min-height: 4.8rem;
   padding: 0.65rem 0.8rem;
-  border-color: #c9d0dc;
+  border-color: var(--line);
   border-inline-start-width: 0.28rem;
   border-radius: 0.3rem;
   background: white;
-  box-shadow: 0 1px 2px rgb(22 29 40 / 7%);
+  box-shadow: 0 1px 2px rgb(38 24 22 / 7%);
 }
 .schedule-card--variation,
 .schedule-card--substitution { box-shadow: none; }
 .schedule-card--substitution { --schedule-entry-color: #e6c33d; }
 .schedule-card__time { margin: 0 0 0.15rem; font-size: 0.7rem; }
 .schedule-card h3 { margin: 0.1rem 0; font-size: 1rem; line-height: 1.2; overflow-wrap: anywhere; }
-.schedule-card__activity { margin: 0.2rem 0; color: #687180; font-size: 0.72rem; overflow-wrap: anywhere; }
-.schedule-card__room { display: inline-flex; align-items: center; gap: 0.3rem; width: fit-content; margin: 0.25rem 0 0; padding: 0.15rem 0.45rem; border-radius: 999px; color: #0753a6; background: #eaf2fc; font-size: 0.7rem; font-weight: 700; }
+.schedule-card__activity { margin: 0.2rem 0; color: var(--muted); font-size: 0.72rem; overflow-wrap: anywhere; }
+.schedule-card__room { display: inline-flex; align-items: center; gap: 0.3rem; width: fit-content; margin: 0.25rem 0 0; padding: 0.15rem 0.45rem; border-radius: 999px; color: var(--brand); background: var(--surface-soft); font-size: 0.7rem; font-weight: 700; }
 .schedule-card__room-compact { display: none; }
 .schedule-card__notes { display: none; }
 .schedule-card__substitution { margin: 0.3rem 0 0; font-size: 0.72rem; }
 .schedule-badge { font-size: 0.58rem; }
 
 .school-day-grid { min-width: 0; max-width: 100%; }
-.school-day-grid__scroller { width: 100%; max-width: 100vw; height: calc(100dvh - 14.5rem); overflow: auto; overscroll-behavior: contain; touch-action: pan-x pan-y; background: #f8f9fb; }
+.school-day-grid__scroller { width: 100%; max-width: 100vw; height: calc(100dvh - 14.5rem); overflow: auto; overscroll-behavior: contain; touch-action: pan-x pan-y; background: var(--page); }
 .school-day-grid__headers { grid-template-columns: 3.7rem repeat(var(--class-count), minmax(3.55rem, 1fr)); min-width: calc(3.7rem + var(--class-count) * 3.55rem); }
 .school-day-grid__body { grid-template-columns: 3.7rem minmax(calc(var(--class-count) * 3.55rem), 1fr); min-width: calc(3.7rem + var(--class-count) * 3.55rem); }
 .school-day-grid__time-corner,
@@ -2050,7 +2118,7 @@ a {
 .school-day-grid__classes { min-width: calc(var(--class-count) * 3.55rem); }
 .school-day-grid__entry { left: calc((var(--entry-column) - 1) * 100% / var(--class-count) + 0.12rem); width: calc(var(--entry-columns) * 100% / var(--class-count) - 0.24rem); padding: 0.12rem; }
 .school-day-grid__break { z-index: 1; opacity: 0.55; font-size: 0; }
-.school-compact-entry { position: relative; z-index: 2; display: grid; height: 100%; place-items: center; overflow: visible; border: 1px solid #d5dae2; border-inline-start: 0.24rem solid #0753a6; border-radius: 0.2rem; background: white; }
+.school-compact-entry { position: relative; z-index: 2; display: grid; height: 100%; place-items: center; overflow: visible; border: 1px solid var(--line); border-inline-start: 0.24rem solid var(--brand); border-radius: 0.2rem; background: white; }
 .school-compact-entry--substitution { border-inline-start-color: #efd048; background: #fffaf0; }
 .school-compact-entry--variation { border-inline-start-color: #ba2028; }
 .school-compact-entry > span { display: none; }
@@ -2069,12 +2137,12 @@ a {
   grid-template-columns: repeat(3, 1fr);
   min-height: 4.25rem;
   padding: 0.35rem max(0.5rem, env(safe-area-inset-right)) max(0.35rem, env(safe-area-inset-bottom)) max(0.5rem, env(safe-area-inset-left));
-  border-top: 1px solid #c8cfda;
+  border-top: 1px solid var(--line);
   background: rgb(255 255 255 / 98%);
 }
-.mobile-mode-navigation .btn { display: grid; min-height: 3.4rem; place-items: center; gap: 0; padding: 0.25rem; border: 0; color: #4d5563; background: transparent; font-size: 1rem; }
+.mobile-mode-navigation .btn { display: grid; min-height: 3.4rem; place-items: center; gap: 0; padding: 0.25rem; border: 0; color: var(--ink); background: transparent; font-size: 1rem; }
 .mobile-mode-navigation .btn strong { font-size: 0.76rem; }
-.mobile-mode-navigation .btn[aria-current="page"] { color: #0753a6; background: #e8f1fd; }
+.mobile-mode-navigation .btn[aria-current="page"] { color: var(--brand); background: var(--surface-soft); }
 .public-page + .site-footer { display: none; }
 
 .sr-only {
@@ -2095,12 +2163,11 @@ a {
     position: sticky;
     display: grid;
     grid-template-columns: auto minmax(0, 1fr);
-    min-height: 4rem;
+    min-height: 4.5rem;
     padding: 0 1.75rem;
     backdrop-filter: blur(12px);
   }
-  .public-brand { font-size: 1.15rem; }
-  .public-app-bar > .public-brand { align-self: start; margin-top: 1.15rem; }
+  .public-app-bar > .public-brand { align-self: center; }
   .schedule-navigation {
     display: grid;
     grid-template-columns: minmax(20rem, 1fr) auto;
@@ -2109,20 +2176,20 @@ a {
     margin-left: 2.5rem;
   }
   .desktop-mode-navigation { display: flex; gap: 0.2rem; }
-  .desktop-mode-navigation .btn { min-height: 2.5rem; gap: 0.5rem; padding: 0.45rem 0.75rem; border: 0; color: #414a58; background: transparent; text-decoration: none; }
-  .desktop-mode-navigation .btn[aria-current="page"] { color: #0753a6; background: #e6eef9; }
+  .desktop-mode-navigation .btn { min-height: 2.5rem; gap: 0.5rem; padding: 0.45rem 0.75rem; border: 0; color: var(--ink); background: transparent; text-decoration: none; }
+  .desktop-mode-navigation .btn[aria-current="page"] { color: var(--brand); background: var(--surface-soft); }
   .contextual-selector { grid-column: 2; }
   .portal-access { grid-column: 2; padding-inline: 0.75rem; }
   .portal-access > span { display: inline; }
   .contextual-selector summary { min-width: 12rem; }
   .date-navigation > .desktop-export-actions { display: flex; align-items: center; justify-content: end; gap: 0.45rem; white-space: nowrap; }
-  .desktop-export-actions > .btn { min-height: 2.35rem; padding: 0.45rem 0.65rem; border-color: #c5ccd7; color: #2d3643; background: transparent; font-size: 0.72rem; text-decoration: none; }
+  .desktop-export-actions > .btn { min-height: 2.35rem; padding: 0.45rem 0.65rem; border-color: var(--line); color: var(--ink); background: transparent; font-size: 0.72rem; text-decoration: none; }
   .share-menu--desktop { display: block; }
-  .share-menu--desktop > summary { width: 2.35rem; height: 2.35rem; color: #394351; background: transparent; }
+  .share-menu--desktop > summary { width: 2.35rem; height: 2.35rem; color: var(--ink); background: transparent; }
   .share-menu--mobile { display: none; }
   .share-menu--desktop .share-sheet { position: absolute; top: calc(100% + 0.45rem); right: 0; bottom: auto; left: auto; display: block; width: 17rem; }
   .share-menu--desktop .share-sheet__backdrop { display: none; }
-  .share-menu--desktop .share-sheet__panel { display: grid; width: 17rem; padding: 0.6rem; border: 1px solid #cbd2dc; border-radius: 0.35rem; box-shadow: 0 0.65rem 1.5rem rgb(22 29 40 / 14%); }
+  .share-menu--desktop .share-sheet__panel { display: grid; width: 17rem; padding: 0.6rem; border: 1px solid var(--line); border-radius: 0.35rem; box-shadow: 0 0.65rem 1.5rem rgb(38 24 22 / 14%); }
   .share-menu--desktop .share-sheet__panel > i,
   .share-menu--desktop .share-sheet h2,
   .share-menu--desktop .share-sheet__cancel { display: none; }
@@ -2136,7 +2203,7 @@ a {
     margin: 0;
     padding: 1rem 0 0.4rem;
     border: 0;
-    background: #f8f9fb;
+    background: var(--page);
   }
   .date-navigation > div:not(.desktop-export-actions),
   .published-date-selector > summary { justify-items: start; }
@@ -2154,10 +2221,10 @@ a {
     max-width: 100%;
     padding: 1.35rem 1.9rem 0.5rem;
   }
-  .schedule-heading h1 { color: #17191d; font-size: 1.55rem; line-height: 1.2; }
+  .schedule-heading h1 { color: var(--ink); font-size: 1.55rem; line-height: 1.2; }
   .public-schedule-shell--all .schedule-heading h1 { position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0; }
   .public-schedule-shell--all .schedule-heading { justify-content: flex-end; }
-  .publication-meta { color: #687180; font-size: 0.72rem; text-align: right; }
+  .publication-meta { color: var(--muted); font-size: 0.72rem; text-align: right; }
   .school-selector { display: none; }
   .mobile-day,
   .school-mobile-view { display: none; }
@@ -2165,17 +2232,17 @@ a {
   .school-desktop-view { display: block; }
   .published-schedule { padding: 0 1.9rem 1.5rem; }
   .desktop-week-grid > h2 { display: none; }
-  .desktop-week-grid__scroller { max-height: calc(100vh - 13rem); overflow: auto; background: #f8f9fb; }
+  .desktop-week-grid__scroller { max-height: calc(100vh - 13rem); overflow: auto; background: var(--page); }
   .desktop-week-grid__header,
   .desktop-week-grid__body { grid-template-columns: 4.2rem repeat(var(--day-count), minmax(min(9rem, calc((100vw - 10rem) / var(--day-count))), 1fr)); min-width: 0; }
   .desktop-week-grid__days { grid-column: 2 / -1; width: 100%; grid-template-columns: repeat(var(--day-count), minmax(min(9rem, calc((100vw - 10rem) / var(--day-count))), 1fr)); }
-  .desktop-week-grid__header { background: rgb(248 249 251 / 96%); }
-  .desktop-week-grid__header h3 { padding: 0.55rem 0.7rem; border-inline-start: 0; border-bottom: 2px solid #c3cad5; color: #3e4755; text-align: left; }
-  .desktop-week-grid__header h3:first-of-type { border-bottom-color: #0753a6; }
+  .desktop-week-grid__header { background: rgb(255 248 247 / 96%); }
+  .desktop-week-grid__header h3 { padding: 0.55rem 0.7rem; border-inline-start: 0; border-bottom: 2px solid var(--line); color: var(--ink); text-align: left; }
+  .desktop-week-grid__header h3:first-of-type { border-bottom-color: var(--brand); }
   .desktop-week-grid__day { border-inline-start: 0; margin-left: 0.7rem; }
   .desktop-week-grid__entry { right: 0; left: 0; padding: 0.2rem 0; }
   .desktop-week-grid__entry .schedule-card { overflow: hidden; }
-  .desktop-week-grid__break { right: 0; left: 0; margin: 0.15rem 0; border: 1px dashed #c5ccd7; color: #6d7480; background: #f4f5f7; text-transform: uppercase; }
+  .desktop-week-grid__break { right: 0; left: 0; margin: 0.15rem 0; border: 1px dashed var(--line); color: var(--muted); background: var(--surface-soft); text-transform: uppercase; }
   .desktop-week-grid .schedule-card { min-height: 0; padding: 0.55rem 0.65rem; }
   .desktop-week-grid .schedule-card { position: relative; }
   .desktop-week-grid .schedule-badge { position: absolute; top: 0.3rem; right: 0.3rem; }
@@ -2196,18 +2263,18 @@ a {
   .school-desktop-view { display: grid; gap: 2.2rem; }
   .school-desktop-view .school-day-grid > h2 { display: block; margin: 0 0 0.65rem; padding: 0; border: 0; background: transparent; font-size: 1.05rem; }
   .school-desktop-view .school-day-grid--mobile { display: none; }
-  .school-horizontal-grid { display: block; min-width: 56rem; border: 1px solid #c8cfda; background: white; }
+  .school-horizontal-grid { display: block; min-width: 56rem; border: 1px solid var(--line); background: white; }
   .school-horizontal-grid__header,
   .school-horizontal-grid__row { display: grid; grid-template-columns: 5.8rem minmax(0, 1fr); }
-  .school-horizontal-grid__header { min-height: 2.8rem; border-bottom: 1px solid #c8cfda; background: #e5e8ec; }
+  .school-horizontal-grid__header { min-height: 2.8rem; border-bottom: 1px solid var(--line); background: var(--surface-soft); }
   .school-horizontal-grid__header > span,
-  .school-horizontal-grid__row > h3 { display: grid; margin: 0; place-items: center; border-right: 1px solid #c8cfda; font-size: 0.75rem; }
+  .school-horizontal-grid__row > h3 { display: grid; margin: 0; place-items: center; border-right: 1px solid var(--line); font-size: 0.75rem; }
   .school-horizontal-grid__header > div,
   .school-horizontal-grid__track { position: relative; min-height: 4rem; }
   .school-horizontal-grid__header > div { min-height: 2.8rem; }
-  .school-horizontal-grid__header time { position: absolute; left: calc((var(--time-minute) - var(--grid-start-minute)) / var(--grid-duration) * 100%); top: 50%; transform: translate(-50%, -50%); color: #3e4755; font-size: 0.68rem; font-weight: 700; }
-  .school-horizontal-grid__boundary { position: absolute; z-index: 0; top: 0; bottom: 0; left: calc((var(--time-minute) - var(--grid-start-minute)) / var(--grid-duration) * 100%); width: 1px; background: #d5dae2; pointer-events: none; }
-  .school-horizontal-grid__row { border-bottom: 1px solid #d5dae2; }
+  .school-horizontal-grid__header time { position: absolute; left: calc((var(--time-minute) - var(--grid-start-minute)) / var(--grid-duration) * 100%); top: 50%; transform: translate(-50%, -50%); color: var(--ink); font-size: 0.68rem; font-weight: 700; }
+  .school-horizontal-grid__boundary { position: absolute; z-index: 0; top: 0; bottom: 0; left: calc((var(--time-minute) - var(--grid-start-minute)) / var(--grid-duration) * 100%); width: 1px; background: var(--line); pointer-events: none; }
+  .school-horizontal-grid__row { border-bottom: 1px solid var(--line); }
   .school-horizontal-grid__row:last-child { border-bottom: 0; }
   .school-horizontal-grid__entry { position: absolute; top: 0.25rem; bottom: 0.25rem; left: calc((var(--entry-start) - var(--grid-start-minute)) / var(--grid-duration) * 100% + 0.16rem); width: calc(var(--entry-duration) / var(--grid-duration) * 100% - 0.32rem); min-width: 2.4rem; }
   .school-horizontal-grid .school-compact-entry { display: grid; place-items: start; align-content: center; padding: 0.35rem 0.5rem; }
@@ -2226,14 +2293,14 @@ a {
 
 @media (max-width: 59.999rem) {
   .admin-nav { display: none; }
-  .admin-bottom-nav { position: fixed; z-index: 70; right: 0; bottom: 0; left: 0; display: flex; min-height: 4.25rem; padding-bottom: env(safe-area-inset-bottom); overflow-x: auto; border-top: 1px solid #c8cfda; background: rgb(255 255 255 / 98%); }
+  .admin-bottom-nav { position: fixed; z-index: 70; right: 0; bottom: 0; left: 0; display: flex; min-height: 4.25rem; padding-bottom: env(safe-area-inset-bottom); overflow-x: auto; border-top: 1px solid var(--line); background: rgb(255 255 255 / 98%); }
   .admin-header,
   .admin-account { align-items: center; flex-direction: row; }
   .admin-header { padding: 0.65rem 1rem; }
   .admin-header p { display: none; }
   .admin-account > span { display: block; max-width: 8rem; overflow: hidden; font-size: 0.72rem; text-overflow: ellipsis; white-space: nowrap; }
-  .admin-bottom-nav a { display: grid; flex: 1 0 4.8rem; min-width: 0; place-items: center; gap: 0.2rem; padding: 0.35rem 0.2rem; overflow: hidden; color: #4d5665; font-size: 0.62rem; font-weight: 750; text-align: center; text-decoration: none; }
-  .admin-bottom-nav a[aria-current="page"] { color: #0753a6; background: #e7effa; }
+  .admin-bottom-nav a { display: grid; flex: 1 0 4.8rem; min-width: 0; place-items: center; gap: 0.2rem; padding: 0.35rem 0.2rem; overflow: hidden; color: var(--ink); font-size: 0.62rem; font-weight: 750; text-align: center; text-decoration: none; }
+  .admin-bottom-nav a[aria-current="page"] { color: var(--brand); background: var(--surface-soft); }
   .admin-bottom-nav a svg { font-size: 1.15rem; }
 }
 
@@ -2256,7 +2323,7 @@ a {
   border: 1px solid var(--line);
   border-radius: 1rem;
   background: var(--paper);
-  box-shadow: 0 0.55rem 1.6rem rgb(23 36 61 / 6%);
+  box-shadow: 0 0.55rem 1.6rem rgb(38 24 22 / 6%);
 }
 
 .admin-publications-page__heading,
diff --git a/src/app/login/page.tsx b/src/app/login/page.tsx
index 37f66a6..f9fa0ab 100644
--- a/src/app/login/page.tsx
+++ b/src/app/login/page.tsx
@@ -1,4 +1,5 @@
 import Link from "next/link";
+import Image from "next/image";
 import type { Metadata } from "next";
 
 import { safeAdminCallbackPath } from "@/modules/auth/admin-ui";
@@ -44,8 +45,12 @@ export default async function LoginPage({ searchParams }: LoginPageProps) {
   return (
     <main className="login-page">
       <section className="login-card" aria-labelledby="login-title">
-        <div className="login-card__brand" aria-hidden="true">R</div>
-        <p className="eyebrow">Orario Rainerum</p>
+        <Link aria-label="Orario Rainerum" className="login-card__brand portal-brand" href="/orario">
+          <Image alt="" aria-hidden="true" className="portal-brand__logo portal-brand__logo--full" height={392} src="/rainerum-logo-full.png" unoptimized width={900} />
+          <Image alt="" aria-hidden="true" className="portal-brand__logo portal-brand__logo--mark" height={256} src="/rainerum-logo-mark.png" unoptimized width={256} />
+          <span className="portal-brand__service">Orario</span>
+        </Link>
+        <p className="eyebrow">Accesso personale</p>
         <h1 id="login-title">Area riservata al personale</h1>
         <p>
           Accedi con l’account Google Workspace del Rainerum. L’area è
diff --git a/src/app/lyco-ui.css b/src/app/lyco-ui.css
index 649b093..47a83c2 100644
--- a/src/app/lyco-ui.css
+++ b/src/app/lyco-ui.css
@@ -6,11 +6,13 @@
   --surface-elevated: var(--surface);
   --surface-floating: var(--surface);
   --color-bg-root: var(--surface-root);
+  --color-bg-subtle: var(--surface-soft);
   --color-bg-surface: var(--surface-base);
   --color-surface-base: var(--surface);
   --color-surface-elevated: var(--surface-elevated);
   --color-surface-floating: var(--surface-floating);
   --color-surface-hover: var(--surface-soft);
+  --color-surface-active: var(--surface-soft);
   --color-text-primary: var(--ink);
   --color-text-secondary: var(--muted);
   --color-text-muted: var(--muted);
@@ -21,9 +23,26 @@
   --color-primary-hover: var(--brand-hover);
   --color-primary-active: var(--brand-hover);
   --color-primary-dim: rgb(184 24 27 / 15%);
-  --color-danger: #ad2336;
-  --color-danger-hover: #ca3045;
-  --color-danger-active: #861827;
+  --color-success: var(--success);
+  --color-success-hover: #17643e;
+  --color-success-active: #125032;
+  --color-success-dim: rgb(31 122 77 / 14%);
+  --color-warning: var(--warning);
+  --color-warning-hover: #7d4f00;
+  --color-warning-active: #633f00;
+  --color-warning-dim: rgb(154 98 0 / 15%);
+  --color-danger: var(--danger);
+  --color-danger-hover: #861827;
+  --color-danger-active: #6d1420;
+  --color-danger-dim: rgb(165 29 45 / 14%);
+  --color-info: var(--focus);
+  --color-info-hover: #00335d;
+  --color-info-active: #002747;
+  --color-info-dim: rgb(0 64 117 / 14%);
+  --color-text-success: var(--success);
+  --color-text-warning: var(--warning);
+  --color-text-danger: var(--danger);
+  --color-text-info: var(--focus);
   --font-family-base: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
   --font-size-base: 1rem;
   --font-size-sm: 0.875rem;
@@ -32,7 +51,14 @@
   --border-width: 1px;
   --border-width-2: 2px;
   --border-width-3: 3px;
+  --radius-sm: 0.5rem;
+  --radius-md: 0.75rem;
+  --radius-lg: 1rem;
+  --radius-xl: 1rem;
   --radius-full: 999px;
   --z-dropdown: 20;
+  --shadow-sm: 0 1px 2px rgb(38 24 22 / 8%);
+  --shadow-md: 0 10px 30px rgb(85 49 43 / 8%);
+  --shadow-lg: 0 10px 30px rgb(85 49 43 / 8%);
   --shadow-xl: 0 1rem 2rem rgb(38 24 22 / 16%);
 }
diff --git a/src/ui/admin/admin-shell.tsx b/src/ui/admin/admin-shell.tsx
index 74f1659..0c4e3ac 100644
--- a/src/ui/admin/admin-shell.tsx
+++ b/src/ui/admin/admin-shell.tsx
@@ -1,3 +1,4 @@
+import Image from "next/image";
 import Link from "next/link";
 import type { ReactNode } from "react";
 
@@ -28,8 +29,12 @@ export function AdminShell({
   return (
     <div className="admin-app">
       <header className="admin-header">
-        <div>
-          <Link className="admin-brand" href="/admin">Orario Rainerum</Link>
+        <div className="admin-identity">
+          <Link aria-label="Orario Rainerum" className="admin-brand portal-brand" href="/admin">
+            <Image alt="" aria-hidden="true" className="portal-brand__logo portal-brand__logo--full" height={392} src="/rainerum-logo-full.png" unoptimized width={900} />
+            <Image alt="" aria-hidden="true" className="portal-brand__logo portal-brand__logo--mark" height={256} src="/rainerum-logo-mark.png" unoptimized width={256} />
+            <span className="portal-brand__service">Orario</span>
+          </Link>
           <p>Area personale</p>
         </div>
         <div className="admin-account">
diff --git a/src/ui/legal/privacy.tsx b/src/ui/legal/privacy.tsx
index 56ed3f7..a361b9a 100644
--- a/src/ui/legal/privacy.tsx
+++ b/src/ui/legal/privacy.tsx
@@ -1,7 +1,15 @@
+import Image from "next/image";
+import Link from "next/link";
+
 export function PrivacyNotice() {
   return (
     <article className="legal-document">
       <header>
+        <Link aria-label="Orario Rainerum" className="legal-brand portal-brand" href="/orario">
+          <Image alt="" aria-hidden="true" className="portal-brand__logo portal-brand__logo--full" height={392} src="/rainerum-logo-full.png" unoptimized width={900} />
+          <Image alt="" aria-hidden="true" className="portal-brand__logo portal-brand__logo--mark" height={256} src="/rainerum-logo-mark.png" unoptimized width={256} />
+          <span className="portal-brand__service">Orario</span>
+        </Link>
         <p className="eyebrow">Versione 1.0 · efficace dal 26 agosto 2026</p>
         <h1>Informativa privacy dell’Orario Rainerum</h1>
         <p>Informativa specifica per la piattaforma di pubblicazione e gestione dell’orario.</p>
diff --git a/src/ui/legal/terms.tsx b/src/ui/legal/terms.tsx
index 08e6f42..4ca2a8c 100644
--- a/src/ui/legal/terms.tsx
+++ b/src/ui/legal/terms.tsx
@@ -1,7 +1,15 @@
+import Image from "next/image";
+import Link from "next/link";
+
 export function TermsOfService() {
   return (
     <article className="legal-document">
       <header>
+        <Link aria-label="Orario Rainerum" className="legal-brand portal-brand" href="/orario">
+          <Image alt="" aria-hidden="true" className="portal-brand__logo portal-brand__logo--full" height={392} src="/rainerum-logo-full.png" unoptimized width={900} />
+          <Image alt="" aria-hidden="true" className="portal-brand__logo portal-brand__logo--mark" height={256} src="/rainerum-logo-mark.png" unoptimized width={256} />
+          <span className="portal-brand__service">Orario</span>
+        </Link>
         <p className="eyebrow">Versione 1.0 · efficaci dal 26 agosto 2026</p>
         <h1>Termini di servizio dell’Orario Rainerum</h1>
       </header>
diff --git a/src/ui/timetable/public-schedule-shell.tsx b/src/ui/timetable/public-schedule-shell.tsx
index 0677044..399df37 100644
--- a/src/ui/timetable/public-schedule-shell.tsx
+++ b/src/ui/timetable/public-schedule-shell.tsx
@@ -1,3 +1,4 @@
+import Image from "next/image";
 import type { AdjacentPublishedWeekAvailability, PublishedCalendarDate, PublishedScheduleView } from "@/modules/publications/queries";
 import type { ReactNode } from "react";
 import type { ScheduleAudience } from "./model";
@@ -16,7 +17,11 @@ export function PublicScheduleShell({ adjacentWeeks, audience, availableDates, c
   const school = schedule.catalog.schools.find(({ code }) => code === selectedSchool) ?? schedule.catalog.schools[0];
   return <div className={`public-schedule-shell public-schedule-shell--${audience.kind}`}>
     <header className="public-app-bar">
-      <a className="public-brand" href={`/orario/${schedule.weekStart}`}>Orario Rainerum</a>
+      <a aria-label="Orario Rainerum" className="public-brand portal-brand" href={`/orario/${schedule.weekStart}`}>
+        <Image alt="" aria-hidden="true" className="portal-brand__logo portal-brand__logo--full" height={392} src="/rainerum-logo-full.png" unoptimized width={900} />
+        <Image alt="" aria-hidden="true" className="portal-brand__logo portal-brand__logo--mark" height={256} src="/rainerum-logo-mark.png" unoptimized width={256} />
+        <span className="portal-brand__service">Orario</span>
+      </a>
       <ScheduleNavigation adjacentWeeks={adjacentWeeks} audience={audience} availableDates={availableDates} completeSchedule={schedule} selectedDay={selectedDay} selectedSchool={selectedSchool} />
     </header>
     <section className="schedule-heading">
diff --git a/tests/unit/auth/admin-pages.test.tsx b/tests/unit/auth/admin-pages.test.tsx
index d55f764..c9b7765 100644
--- a/tests/unit/auth/admin-pages.test.tsx
+++ b/tests/unit/auth/admin-pages.test.tsx
@@ -12,6 +12,8 @@ import HomePage from "@/app/page";
 import LoginPage from "@/app/login/page";
 import { AdminDashboard } from "@/ui/admin/admin-dashboard";
 import { AdminShell } from "@/ui/admin/admin-shell";
+import { PrivacyNotice } from "@/ui/legal/privacy";
+import { TermsOfService } from "@/ui/legal/terms";
 
 describe("staff entry pages", () => {
   it("exposes the public timetable and staff login from home", () => {
@@ -37,6 +39,32 @@ describe("staff entry pages", () => {
     expect(html).toContain("non è autorizzato");
     expect(html).toContain("vicepreside");
   });
+
+  it("uses the official responsive Rainerum identity on login", async () => {
+    const page = await LoginPage({ searchParams: Promise.resolve({}) });
+    const html = renderToStaticMarkup(page);
+
+    expect(html).toContain('aria-label="Orario Rainerum"');
+    expect(html).toContain('href="/orario"');
+    expect(html).toContain('src="/rainerum-logo-full.png"');
+    expect(html).toContain('src="/rainerum-logo-mark.png"');
+    expect(html).toContain('class="portal-brand__service">Orario</span>');
+  });
+});
+
+describe("public legal identity", () => {
+  it.each([
+    ["privacy", <PrivacyNotice key="privacy" />],
+    ["terms", <TermsOfService key="terms" />],
+  ])("links the %s notice to the public timetable with the official responsive marks", (_name, notice) => {
+    const html = renderToStaticMarkup(notice);
+
+    expect(html).toContain('aria-label="Orario Rainerum"');
+    expect(html).toContain('href="/orario"');
+    expect(html).toContain('src="/rainerum-logo-full.png"');
+    expect(html).toContain('src="/rainerum-logo-mark.png"');
+    expect(html).toContain('class="portal-brand__service">Orario</span>');
+  });
 });
 
 describe("role-aware admin experience", () => {
@@ -55,6 +83,18 @@ describe("role-aware admin experience", () => {
     expect(html).toContain("Esci");
   });
 
+  it("uses one accessible admin brand link with the official responsive marks", () => {
+    const html = renderToStaticMarkup(
+      <AdminShell name="Dirigente Test" roles="admin"><p>Contenuto</p></AdminShell>,
+    );
+
+    expect(html).toContain('aria-label="Orario Rainerum"');
+    expect(html).toContain('href="/admin"');
+    expect(html).toContain('src="/rainerum-logo-full.png"');
+    expect(html).toContain('src="/rainerum-logo-mark.png"');
+    expect(html).toContain('class="portal-brand__service">Orario</span>');
+  });
+
   it("keeps every admin operation and account identity in the mobile shell markup", () => {
     const html = renderToStaticMarkup(<AdminShell name="Dirigente Test" roles="admin"><p>Contenuto</p></AdminShell>);
 
diff --git a/tests/unit/publications/public-page.test.tsx b/tests/unit/publications/public-page.test.tsx
index 8d720e1..ff54457 100644
--- a/tests/unit/publications/public-page.test.tsx
+++ b/tests/unit/publications/public-page.test.tsx
@@ -245,6 +245,10 @@ describe("public schedule publication identity", () => {
     expect(html).toContain('aria-label="Seleziona giorno"');
     expect(html).toContain('aria-label="Navigazione mobile dell’orario"');
     expect(html).toContain('class="public-app-bar"');
+    expect(html).toContain('aria-label="Orario Rainerum"');
+    expect(html).toContain('src="/rainerum-logo-full.png"');
+    expect(html).toContain('src="/rainerum-logo-mark.png"');
+    expect(html).toContain('class="portal-brand__service">Orario</span>');
     expect(html).toContain('class="mobile-mode-navigation"');
     expect(html).toContain('class="share-sheet"');
     expect(html).toContain("Orario Rainerum");
